import { initialState as topics } from '../reducers/topics';
import { initialState as screenSize } from '../reducers/screenSize';
import { initialState as sign } from '../reducers/sign';
import { initialState as auth } from '../reducers/auth';
import { initialState as profile } from '../reducers/profile';
import { initialState as story } from '../reducers/story';
import { initialState as responses } from '../reducers/responses';
import { initialState as currentTopic } from '../reducers/currentTopic';
import { initialState as notifications } from '../reducers/notifications';

export default {
  screenSize,
  sign,
  auth,
  topics,
  profile,
  story,
  responses,
  currentTopic,
  notifications,
};
